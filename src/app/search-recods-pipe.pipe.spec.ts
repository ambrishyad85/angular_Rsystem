import { SearchRecodsPipePipe } from './search-recods-pipe.pipe';

describe('SearchRecodsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchRecodsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
